<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Billing Info Inventory</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/styles/home.css" rel="stylesheet" type="text/css">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container mt-5">
        <h2 class="mb-4">Billing Info Inventory</h2>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Customer ID</th>
                    <th>Customer Name</th>
                    <th>Address</th>
                    <th>Contact Number</th>
                    <th>Member ID</th>
                    <th>Deletion</th>
                    <th>Updation</th>
                </tr>
            </thead>
            <tbody>
                <% bills.forEach(bill => { %>
                    <tr>
                        <td><%= bill.customer_id %></td>
                        <td><%= bill.customer_name %></td>
                        <td><%= bill.address %></td>
                        <td><%= bill.contact_number %></td>
                        <td><%= bill.member_id %></td>
                        
                        <td>
                            <button type="button" class="btn btn-danger delete-btn" data-id="<%= bill.customer_id %>">Delete</button>
                        </td>
                        <td>
                            <button type="button" class="btn btn-success edit-btn" data-id="<%= bill.customer_id %>">Edit</button>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

</body>
</html>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        // Delete button functionality
        document.querySelectorAll('.delete-btn').forEach(button => {
            button.addEventListener('click', async (event) => {
                event.preventDefault(); // Prevent any default action
                
                const customerID = button.getAttribute('data-id');

                // Confirm the deletion
                const confirmed = confirm(`Are you sure you want to delete Customer ID ${customerID}?`);
                if (!confirmed) return;

                try {
                    // Send AJAX request to delete the record
                    const response = await fetch(`/bills/delete/${customerID}`, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                    });

                    if (response.ok) {
                        // Remove the row from the table
                        button.closest('tr').remove();
                        alert('Bill deleted successfully!');
                    } else {
                        const errorData = await response.json();
                        console.error('Error:', errorData.error);
                        alert('Failed to delete the bill. Please try again.');
                    }
                } catch (error) {
                    console.error('AJAX Error:', error);
                    alert('An error occurred while deleting the bill.');
                }
            });
        });

        // Edit button functionality
        document.querySelectorAll('.edit-btn').forEach(button => {
            button.addEventListener('click', (event) => {
                event.preventDefault(); // Prevent any default action

                const customerID = button.getAttribute('data-id');

                // Redirect to the edit page
                window.location.href = `/bills/update/${customerID}`;
            });
        });
    });
</script>
